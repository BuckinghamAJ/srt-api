---
applications:
- name: srt-server-dev
  memory: 512M
  disk_quota: 512M
#  health-check-type: process # don't re-enable....move to port 8080 if you have problems
  instances: 1
  env:
    NODE_ENV: clouddev
  services:
    - srt-postgres-dev